var1: int = 3;
var2: int = var1 + 3;
var3: string = "salad";

fun printSalad(s: string): void {
    fun sayHi(x: string): string {
        x1: string = "Hello, ";
        return x1 + x;
    };

    sayHi("" + var3);
};

printSalad(var3);
